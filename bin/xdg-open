#!/bin/sh
case "${1%%:*}" in
http | https | *.pdf)
	exec firefox "$1"
	;;
mailto)
	exec mutt "$1"
	;;
*.mp3 | *.ogg | *.mp4 | *.mkv | *.avi | *.opus)
	exec mpv "$1"
	;;
*)
	exec /usr/bin/xdg-open "$@"
	;;
esac
