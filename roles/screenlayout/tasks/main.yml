---
- name: needed dirs
  file:
    name=~/.screenlayout
    state=directory

- name: copy layout files
  copy:
    src={{ item }}
    dest=~/.screenlayout/{{ item | basename }}
    mode=755
  with_fileglob:
    - ./*
